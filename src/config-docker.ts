import {IConfig} from "./types";

export const config: IConfig = {
    mongoUser: "mongo",
    mongoPass: "mongo",
    mongoUrl: "mongo:27017",
    mongoDb: "jordan-scrapes",
    mongoCollection: "amazon-products",
};
